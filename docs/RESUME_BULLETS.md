# Resume Bullets

Three candidates, ranked. Each is one line, truthful, and defensible against
the actual code in this repository.

## #1 (strongest)

Built a Java transaction-ledger module with idempotent request handling, HMAC-SHA256 tamper detection, and thread-safe concurrency control, validated by 85 JUnit tests including deterministic multi-threaded stress tests.

## #2

Designed and implemented a normalized SQL schema (PostgreSQL-compatible, tested against H2) with foreign keys, check constraints, and unique-constraint-based idempotency, enforced via JDBC transactions with proven rollback-on-failure behavior.

## #3

Developed an Android application in Java with Firebase Authentication and Realtime Database, including a finite-state-machine domain model, input-validating constructors, and a CI pipeline running 140 automated tests on every push.
